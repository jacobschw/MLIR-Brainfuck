<!-- Autogenerated by mlir-tblgen; don't manually edit -->
### `Bf.decrement` (bf::Decrement)

_Decrements the current cell._


Syntax:

```
operation ::= `Bf.decrement` attr-dict
```


### `Bf.increment` (bf::Increment)

_Increments the current cell._


Syntax:

```
operation ::= `Bf.increment` attr-dict
```


### `Bf.input` (bf::Input)

_Input an ASCI value and write it to the current cell._


Syntax:

```
operation ::= `Bf.input` attr-dict
```


### `Bf.loop` (bf::Loop)

_Brainfuck loop in respect to the Brainfuck loop condition: current cell != 0._


Syntax:

```
operation ::= `Bf.loop` attr-dict-with-keyword $body
```


Traits: HasOnlyGraphRegion, NoTerminator, SingleBlock

Interfaces: RegionKindInterface

### `Bf.module` (bf::Module)

_A Brainfuck Module serving as root._


Syntax:

```
operation ::= `Bf.module` attr-dict-with-keyword $body
```


Traits: HasOnlyGraphRegion, IsolatedFromAbove, NoTerminator, SingleBlock

Interfaces: RegionKindInterface

### `Bf.output` (bf::Output)

_Outputs the current cell in ASCI format._


Syntax:

```
operation ::= `Bf.output` attr-dict
```


### `Bf.shift_left` (bf::ShiftLeft)

_Shift the pointer left._


Syntax:

```
operation ::= `Bf.shift_left` attr-dict
```


### `Bf.shift_right` (bf::ShiftRight)

_Shift the pointer right._


Syntax:

```
operation ::= `Bf.shift_right` attr-dict
```


